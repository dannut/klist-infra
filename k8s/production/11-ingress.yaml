apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kli-frontend-ingress
  namespace: kli
  annotations:
    ingress.cilium.io/loadbalancer-mode: "dedicated"
spec:
  ingressClassName: cilium
  rules:
  - host: kli.st
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kli-frontend-active
            port:
              number: 8080
