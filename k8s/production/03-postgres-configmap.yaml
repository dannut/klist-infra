apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-initdb
  namespace: kli
data:
  # Initializeaza DB-ul dupa primul deploy:
  #   kubectl cp database/01_schema.sql kli/$(kubectl get pod -n kli -l app=postgres -o jsonpath='{.items[0].metadata.name}'):/tmp/
  #   kubectl cp database/02_seed.sql   kli/$(kubectl get pod -n kli -l app=postgres -o jsonpath='{.items[0].metadata.name}'):/tmp/
  #   kubectl exec -n kli deploy/postgres -- psql -U kli_user -d kli_db -f /tmp/01_schema.sql
  #   kubectl exec -n kli deploy/postgres -- psql -U kli_user -d kli_db -f /tmp/02_seed.sql
  placeholder: "see comments above for db init"
