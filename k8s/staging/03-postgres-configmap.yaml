apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-initdb
  namespace: kli-staging
data:
  # Initializeaza DB-ul dupa primul deploy:
  #   kubectl cp database/01_schema.sql kli-staging/$(kubectl get pod -n kli-staging -l app=postgres -o jsonpath='{.items[0].metadata.name}'):/tmp/
  #   kubectl cp database/02_seed.sql   kli-staging/$(kubectl get pod -n kli-staging -l app=postgres -o jsonpath='{.items[0].metadata.name}'):/tmp/
  #   kubectl exec -n kli-staging deploy/postgres -- psql -U kli_user -d kli_db -f /tmp/01_schema.sql
  #   kubectl exec -n kli-staging deploy/postgres -- psql -U kli_user -d kli_db -f /tmp/02_seed.sql
  placeholder: "see comments above for db init"
